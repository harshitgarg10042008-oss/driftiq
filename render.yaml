services:
  - type: web
    name: driftiq
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node backend/server.js
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000
